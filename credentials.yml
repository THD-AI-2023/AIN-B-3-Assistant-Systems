# This file contains the credentials for the voice & chat platforms
# which your bot is using.

# Rasa NLU provides a default REST endpoint for receiving messages
# over HTTP. This is ideal for connecting your bot to a custom
# frontend. To enable the REST endpoint, add the following:

rest:

# If you are using the Rasa chatbot in a website, you might need to enable CORS.
# You can do this by adding the following to your 'rasa run' command:
# --cors "*"

# Uncomment this if you are using the socket.io connector.
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: false
